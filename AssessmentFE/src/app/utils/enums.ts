/**
 * Defines the specific type of toast message to be rendered
 */
export enum ToastType {
  Error,
  Warning,
  Success,
}

/**
 * Defines the type of the value for the input fields
 */
export enum InputValueType {
  TextArea = "textarea",
  Number = "number",
  Date = "date",
  Text = "text",
  Selection = "selection"
}
