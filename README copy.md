// TODO: integrare con il readme già esistente

comando per installare le dipendenze: npm i

per la SWAPI: https://swapi.py4e.com/ ho usato un mirror perché l'URL principale non funziona (certificati non validi)